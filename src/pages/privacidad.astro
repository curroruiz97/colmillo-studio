---
/**
 * /privacidad/ — the RGPD/LOPDGDD privacy policy.
 *
 * The wording is in `src/data/legalPages.ts`. It describes this site's actual
 * data flow: a static site with no backend, whose contact form composes a
 * draft in the visitor's own mail client rather than transmitting anything.
 */
import LegalLayout from '@/layouts/LegalLayout.astro';
import { privacyPolicy } from '@/data/legalPages';
---

<LegalLayout doc={privacyPolicy} />
