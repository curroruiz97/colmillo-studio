---
/**
 * /aviso-legal/ — the LSSI-CE notice.
 *
 * The wording is in `src/data/legalPages.ts`; the route only chooses the
 * document. It ships identically in both builds: it is not provisional copy,
 * it is the real notice with the holder's identity still marked as pending.
 */
import LegalLayout from '@/layouts/LegalLayout.astro';
import { legalNotice } from '@/data/legalPages';
---

<LegalLayout doc={legalNotice} />
