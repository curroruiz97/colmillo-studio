---
/**
 * /cookies/ — what this site stores in the browser, and why there is no banner.
 *
 * The wording is in `src/data/legalPages.ts`. Its table lists the single
 * sessionStorage key the site writes; there are no cookies of any kind, so no
 * consent mechanism is published. See `docs/DECISIONS.md` (2026-09-16).
 */
import LegalLayout from '@/layouts/LegalLayout.astro';
import { cookiePolicy } from '@/data/legalPages';
---

<LegalLayout doc={cookiePolicy} />
